apiVersion: v1
kind: Service
metadata:
  name: {{ include "darkness.fullname" . }}
  labels:
    {{- include "darkness.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "darkness.selectorLabels" . | nindent 4 }}
