apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "darkness.fullname" . }}-test-connection"
  labels:
    {{- include "darkness.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "darkness.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
